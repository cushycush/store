.opencode/
store

# IDE / editor
.idea
.vscode

# Tool configs
.mcp.json
.ripgreprc

# Shell profiles (user-local)
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.gitmodules

# Demo fixtures: the patterns above exist to prevent accidental
# commits of real user dotfiles, but the hero-demo uses those names
# deliberately as test content.
!docs/hero-demo/**
